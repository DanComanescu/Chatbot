# Chatbot documentation

Project docs live here.

For development workflow, start with:

- `AGENTS.md` — agent context map
- `_specs/README.md` — Spec-Driven Development pipeline and rules
- `_specs/config.md` — repo-specific SDD configuration

## Calculator (CHATBOT-001)

Open locally:

```text
calculator/index.html
```

Double-click the file or open it in a browser. No build step required.
